# Core API and configuration
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Multi-agent orchestration and LLM provider
langgraph
langchain-core
langchain-openai
openai
google-genai

# Retrieval and document ingestion
qdrant-client
PyMuPDF
firecrawl-py
# Optional: only supports Python <3.14 (skip on newer interpreters — pip install
# will fail on this line alone). knowledge_base.py imports it lazily and falls
# back to a built-in chunker when it's missing, so it is safe to drop this line
# instead of installing it on a Python 3.14+ environment.
semantic-chunkers
sentence-transformers
weaviate-client
grpcio>=1.76.0
python-dotenv

# PostgreSQL persistence (sessions, profiles, evidence, audit logs)
psycopg[binary]

# Evaluation and tests
ragas
pytest

# Optional tracing/observability used by the agent workflow
opentelemetry-api
opentelemetry-sdk


